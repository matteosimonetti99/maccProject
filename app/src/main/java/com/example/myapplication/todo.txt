Tommaso:

- PythonAnywhere
DONE -- Crea API /getqr(id invito) -> sha256(id invito + password db)
DONE-- Crea API /checkqr(sha256(id invito + password db) -> True/False

DONE - Nella pagina i miei inviti e in quella dei dettagli il pulsante in caso di invito accettato deve chiamarsi "get qr"
DONE -- Al click viene fatta una chiamata al db /getqr con l'id dell'invito
DONE --- Il risultato della chiamata viene renderizzato come qr code a schermo

DONE - Crea pulsante "scan qr" su pagina principale.
DONE -- Al click si apre la fotocamera e fa scannerizzare l'immagine
DONE Quando il qr viene riconosciuto viene mandata una richiesta all'API /checkqr con in input l'hash ottenuto
--- Se l'api dà risposta affermativa allora viene mandato un segnale che elimina l'invito sul database

